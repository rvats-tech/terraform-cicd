pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/rvats-tech/terraform-cicd.git'
            }
        }
        stage('terraform init') {
            steps {
                sh 'terraform init' 
            }
        }
    }
}
